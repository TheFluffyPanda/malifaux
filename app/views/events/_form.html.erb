  <div class="container">
    <div class="admin">
    <!-- Your code here -->
    <%= form_for(@event) do |f| %>  
     <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field"> 
	<%= f.label :event %><br>
	<p class="form">Name: <br><%= f.text_field :name %></p>  
	<p class="form">Description: <br><%= f.text_area(:description, size: "50x10") %></p>
	<p class="form">Active?: <br><%= f.check_box :active %></p>
  </div> 
  <div class="actions"> 
    <%= f.submit %> 
  </div> 
<% end %>
    <%= link_to 'Back', events_path %>
  </div>
  </div>
